// Names of service types that are no longer provided by mod-erm-usage-harvester
// Existing usage data providers may still reference them, so their names are kept here for display
const unsupportedServiceTypeNames = {
  cs41: 'Counter-Sushi 4.1',
};

export default unsupportedServiceTypeNames;
